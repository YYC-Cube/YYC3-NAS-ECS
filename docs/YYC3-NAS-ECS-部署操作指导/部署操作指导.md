# YYC³ NAS-ECS 部署操作指导文档

> ***YanYuCloudCube***
> **标语**：言启象限 | 语枢未来
> ***Words Initiate Quadrants, Language Serves as Core for the Future***
> **标语**：万象归元于云枢 | 深栈智启新纪元
> ***All things converge in the cloud pivot; Deep stacks ignite a new era of intelligence***

---

```typescript
/**
 * @fileoverview YYC3-NAS-ECS-部署操作指导
 * @description 提供YYC³ NAS-ECS系统的完整部署操作指导，包括环境准备、配置部署、验证测试和维护监控
 * @author YYC³
 * @version 1.0.0
 * @created 2026-01-07
 * @modified 2026-01-07
 * @copyright Copyright (c) 2026 YYC³
 * @license MIT
 */
```

---

## 📋 目录

1. [文档概述](#1-文档概述)
2. [部署环境要求](#2-部署环境要求)
3. [部署前准备](#3-部署前准备)
4. [部署步骤](#4-部署步骤)
5. [配置说明](#5-配置说明)
6. [验证与测试](#6-验证与测试)
7. [故障排除](#7-故障排除)
8. [维护与监控](#8-维护与监控)
9. [附录](#9-附录)

---

## 1. 文档概述

### 1.1 文档目的

本文档提供YYC³ NAS-ECS系统的完整部署操作指导，确保系统能够在本地NAS和阿里云ECS环境中正确部署和运行。本文档遵循国标化编程规范格式，提供详细的操作步骤和配置说明。

### 1.2 适用范围

- 本地NAS环境部署
- 阿里云ECS云服务器部署
- FRP反向代理配置
- 多环境（开发、测试、生产）部署

### 1.3 术语定义

| 术语 | 定义 |
|------|------|
| NAS | 网络附加存储设备，用于本地服务部署 |
| ECS | 阿里云弹性计算服务，用于云端服务部署 |
| FRP | Fast Reverse Proxy，快速反向代理工具 |
| DDNS | 动态域名解析服务 |

### 1.4 参考文档

- [YYC³ 团队智能应用开发标准规范](../YYC3-NAS-ECS-文档规范.md)
- [快速启动指南](../YYC3-NAS-ECS-快速启动/README-快速启动.md)
- [开发指导文档](../YYC3-NAS-ECS-开发指导/开发指导.md)
- [API多环境设计](../YYC3-NAS-ECS-API文档/YYC3-NAS-ECS-API多环境设计.md)

---

## 2. 部署环境要求

### 2.1 硬件要求

#### 2.1.1 本地NAS环境

| 组件 | 最低配置 | 推荐配置 |
|------|---------|---------|
| CPU | 双核2.0GHz | 四核2.5GHz+ |
| 内存 | 4GB | 8GB+ |
| 存储 | 100GB | 500GB+ SSD |
| 网络 | 100Mbps | 1Gbps |

#### 2.1.2 阿里云ECS环境

| 组件 | 最低配置 | 推荐配置 |
|------|---------|---------|
| 实例规格 | 2 vCPU 4GB | 4 vCPU 8GB+ |
| 存储 | 40GB SSD | 100GB+ SSD |
| 带宽 | 3Mbps | 10Mbps+ |
| 公网IP | 1个 | 1个 |

### 2.2 软件要求

#### 2.2.1 操作系统

- 本地NAS：Linux（推荐Ubuntu 20.04+、Debian 11+）
- 阿里云ECS：Linux（推荐Ubuntu 20.04+、CentOS 8+）

#### 2.2.2 运行环境

| 组件 | 版本要求 |
|------|---------|
| Node.js | >= 18.0.0 |
| npm | >= 9.0.0 |
| Docker | >= 20.10.0 |
| Docker Compose | >= 2.0.0 |
| Git | >= 2.30.0 |
| Redis | >= 6.0.0 |

#### 2.2.3 依赖服务

- FRP Server（阿里云ECS）
- FRP Client（本地NAS）
- Redis服务
- 数据库服务（可选）

### 2.3 网络要求

#### 2.3.1 端口配置

| 服务 | 端口 | 说明 |
|------|------|------|
| API服务 | 6000 | 主API服务端口 |
| LLM服务 | 6002 | 大语言模型服务端口 |
| 邮箱服务 | 6003 | 邮箱系统服务端口 |
| FRP服务 | 6004 | FRP管理服务端口 |
| 前端服务 | 6005 | 前端管理平台端口 |
| NAS服务 | 6006 | NAS文件服务端口 |
| DDNS服务 | 6007 | 动态域名解析服务端口 |
| Redis服务 | 6379 | Redis数据库端口 |

#### 2.3.2 防火墙配置

确保以下端口在防火墙中开放：

```bash
# 阿里云ECS安全组配置
- 入站规则：
  - TCP 6000-6007 允许 0.0.0.0/0
  - TCP 6379 允许 0.0.0.0/0
  - TCP 22 允许 指定IP（SSH访问）
  
- 出站规则：
  - 允许所有
```

---

## 3. 部署前准备

### 3.1 代码获取

#### 3.1.1 克隆代码仓库

```bash
# 克隆项目代码
git clone https://github.com/YYC-Cube/YYC3-NAS-ECS.git
cd yyc3-nas-ecs

# 查看分支
git branch -a

# 切换到指定分支（如需要）
git checkout <branch-name>
```

#### 3.1.2 验证代码完整性

```bash
# 检查项目结构
ls -la

# 验证关键文件存在
test -f package.json && echo "package.json exists"
test -f README.md && echo "README.md exists"
test -d scripts && echo "scripts directory exists"
```

### 3.2 环境配置

#### 3.2.1 安装依赖

```bash
# 安装Node.js依赖
npm install

# 验证安装
npm list --depth=0
```

#### 3.2.2 配置环境变量

```bash
# 复制环境变量模板
cp .env.example .env.local

# 编辑环境变量文件
vim .env.local
```

#### 3.2.3 验证环境变量

```bash
# 检查环境变量文件
cat .env.local

# 验证关键配置
grep -E "^(FRONTEND|API|MAIL|LLM|REDIS)_.*_PORT" .env.local
```

### 3.3 证书准备

#### 3.3.1 SSL证书（可选）

如果需要HTTPS访问，准备SSL证书：

```bash
# 创建证书目录
mkdir -p certs

# 复制证书文件
cp /path/to/cert.pem certs/
cp /path/to/key.pem certs/
```

#### 3.3.2 证书权限设置

```bash
# 设置证书文件权限
chmod 644 certs/cert.pem
chmod 600 certs/key.pem
```

### 3.4 数据库准备

#### 3.4.1 Redis服务配置

```bash
# 启动Redis服务
docker run -d \
  --name redis \
  -p 6379:6379 \
  -v redis-data:/data \
  redis:7-alpine

# 验证Redis运行
docker ps | grep redis
redis-cli ping
```

#### 3.4.2 数据库初始化（如需要）

```bash
# 连接数据库
redis-cli

# 执行初始化脚本（如需要）
# source scripts/init-redis.lua
```

---

## 4. 部署步骤

### 4.1 本地NAS部署

#### 4.1.1 启动所有服务

```bash
# 使用快速启动脚本
./scripts/quick-start.sh

# 或手动启动各服务
npm run start:api
npm run start:mail
npm run start:llm
npm run start:frontend
```

#### 4.1.2 验证服务启动

```bash
# 检查服务状态
./scripts/health-check.sh

# 或手动检查各服务
curl http://localhost:6000/health
curl http://localhost:6002/health
curl http://localhost:6003/health
curl http://localhost:6005
```

#### 4.1.3 查看服务日志

```bash
# 查看所有服务日志
docker-compose logs -f

# 查看特定服务日志
docker-compose logs -f api
docker-compose logs -f mail
docker-compose logs -f llm
```

### 4.2 阿里云ECS部署

#### 4.2.1 连接ECS服务器

```bash
# SSH连接到ECS
ssh root@your-ecs-ip

# 或使用密钥认证
ssh -i /path/to/key.pem root@your-ecs-ip
```

#### 4.2.2 部署FRP服务端

```bash
# 创建FRP目录
mkdir -p /opt/frp
cd /opt/frp

# 下载FRP服务端
wget https://github.com/fatedier/frp/releases/download/v0.52.3/frp_0.52.3_linux_amd64.tar.gz
tar -zxvf frp_0.52.3_linux_amd64.tar.gz
cd frp_0.52.3_linux_amd64

# 配置FRP服务端
cat > frps.toml << 'EOF'
bindPort = 7000
vhostHTTPPort = 80
vhostHTTPSPort = 443

auth.token = "your-auth-token"

webServer.addr = "0.0.0.0"
webServer.port = 7500
webServer.user = "admin"
webServer.password = "admin-password"
EOF

# 启动FRP服务端
./frps -c frps.toml

# 或使用systemd管理
sudo cp frps /usr/local/bin/
sudo cp frps.toml /etc/frp/
sudo tee /etc/systemd/system/frps.service << 'EOF'
[Unit]
Description=frp server
After=network.target

[Service]
Type=simple
ExecStart=/usr/local/bin/frps -c /etc/frp/frps.toml
Restart=on-failure
RestartSec=5s

[Install]
WantedBy=multi-user.target
EOF

sudo systemctl daemon-reload
sudo systemctl enable frps
sudo systemctl start frps
sudo systemctl status frps
```

#### 4.2.3 配置防火墙

```bash
# 使用阿里云安全组配置
# 开放以下端口：
# - 7000 (FRP服务端口)
# - 7500 (FRP管理端口)
# - 80 (HTTP)
# - 443 (HTTPS)
# - 6000-6007 (应用服务端口)
# - 6379 (Redis端口)
```

### 4.3 FRP客户端配置

#### 4.3.1 配置FRP客户端

```bash
# 编辑FRP客户端配置
vim frp/frpc.toml
```

配置内容示例：

```toml
serverAddr = "your-ecs-ip"
serverPort = 7000

auth.token = "your-auth-token"

[[proxies]]
name = "api"
type = "tcp"
localIP = "127.0.0.1"
localPort = 6000
remotePort = 6000

[[proxies]]
name = "llm"
type = "tcp"
localIP = "127.0.0.1"
localPort = 6002
remotePort = 6002

[[proxies]]
name = "mail"
type = "tcp"
localIP = "127.0.0.1"
localPort = 6003
remotePort = 6003

[[proxies]]
name = "frontend"
type = "tcp"
localIP = "127.0.0.1"
localPort = 6005
remotePort = 6005

[[proxies]]
name = "nas"
type = "tcp"
localIP = "127.0.0.1"
localPort = 6006
remotePort = 6006

[[proxies]]
name = "ddns"
type = "tcp"
localIP = "127.0.0.1"
localPort = 6007
remotePort = 6007
```

#### 4.3.2 启动FRP客户端

```bash
# 启动FRP客户端
./frp/frpc -c frp/frpc.toml

# 或使用systemd管理
sudo cp frp/frpc /usr/local/bin/
sudo cp frp/frpc.toml /etc/frp/
sudo tee /etc/systemd/system/frpc.service << 'EOF'
[Unit]
Description=frp client
After=network.target

[Service]
Type=simple
ExecStart=/usr/local/bin/frpc -c /etc/frp/frpc.toml
Restart=on-failure
RestartSec=5s

[Install]
WantedBy=multi-user.target
EOF

sudo systemctl daemon-reload
sudo systemctl enable frpc
sudo systemctl start frpc
sudo systemctl status frpc
```

#### 4.3.3 验证FRP连接

```bash
# 查看FRP客户端日志
journalctl -u frpc -f

# 访问FRP管理面板
open http://your-ecs-ip:7500

# 验证端口转发
curl http://your-ecs-ip:6000/health
curl http://your-ecs-ip:6005
```

### 4.4 多环境部署

#### 4.4.1 开发环境部署

```bash
# 加载开发环境配置
export NODE_ENV=development
source config/.env.development

# 启动开发服务
npm run dev

# 或使用Docker Compose
docker-compose -f docker-compose.dev.yml up -d
```

#### 4.4.2 测试环境部署

```bash
# 加载测试环境配置
export NODE_ENV=test
source config/.env.test

# 启动测试服务
npm run test:start

# 或使用Docker Compose
docker-compose -f docker-compose.test.yml up -d
```

#### 4.4.3 生产环境部署

```bash
# 加载生产环境配置
export NODE_ENV=production
source config/.env.production

# 启动生产服务
npm run start:prod

# 或使用Docker Compose
docker-compose -f docker-compose.prod.yml up -d
```

---

## 5. 配置说明

### 5.1 端口配置

#### 5.1.1 统一端口规范

系统采用6000系列端口作为标准配置：

```bash
# 端口配置文件
cat .env.ports

# 输出示例：
FRONTEND_PORT=6005
API_PORT=6000
MAIL_PORT=6003
LLM_PORT=6002
FRP_PORT=6004
NAS_PORT=6006
DDNS_PORT=6007
REDIS_PORT=6379
```

#### 5.1.2 端口使用说明

| 端口 | 服务 | 用途 |
|------|------|------|
| 6000 | API服务 | 主API接口服务 |
| 6001 | API备用 | API服务备用端口 |
| 6002 | LLM服务 | 大语言模型服务 |
| 6003 | 邮箱服务 | 邮箱系统服务 |
| 6004 | FRP服务 | FRP管理服务 |
| 6005 | 前端服务 | 前端管理平台 |
| 6006 | NAS服务 | NAS文件服务 |
| 6007 | DDNS服务 | 动态域名解析服务 |
| 6379 | Redis服务 | Redis数据库 |

### 5.2 环境变量配置

#### 5.2.1 基础环境变量

```bash
# .env.base 配置示例
NODE_ENV=production
PORT=6000
HOST=0.0.0.0

# 数据库配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# API配置
API_TIMEOUT=30000
API_RATE_LIMIT=1000

# 日志配置
LOG_LEVEL=info
LOG_FILE=/var/log/yyc3/app.log
```

#### 5.2.2 前端配置

```bash
# 前端环境变量配置
VITE_API_BASE_URL=http://localhost:6000
VITE_MAIL_API_URL=http://localhost:6003
VITE_LLM_API_URL=http://localhost:6002
VITE_REDIS_API_URL=http://localhost:6379
VITE_DDNS_API_URL=http://localhost:6007
VITE_FRP_API_URL=http://localhost:6004
VITE_NAS_API_URL=http://localhost:6006

VITE_WS_URL=ws://localhost:6000
```

#### 5.2.3 后端配置

```bash
# 后端服务配置
API_PORT=6000
API_HOST=0.0.0.0
API_CORS_ORIGIN=*

# JWT配置
JWT_SECRET=your-jwt-secret
JWT_EXPIRES_IN=7d

# 邮箱配置
MAIL_PORT=6003
MAIL_HOST=0.0.0.0
MAIL_SMTP_HOST=smtp.example.com
MAIL_SMTP_PORT=587
MAIL_SMTP_USER=user@example.com
MAIL_SMTP_PASS=password

# LLM配置
LLM_PORT=6002
LLM_HOST=0.0.0.0
LLM_API_KEY=your-llm-api-key
LLM_MODEL=gpt-4
```

### 5.3 FRP配置

#### 5.3.1 服务端配置（frps.toml）

```toml
# FRP服务端配置
bindPort = 7000
vhostHTTPPort = 80
vhostHTTPSPort = 443

# 认证配置
auth.token = "your-auth-token"

# 管理面板配置
webServer.addr = "0.0.0.0"
webServer.port = 7500
webServer.user = "admin"
webServer.password = "admin-password"

# 日志配置
log.level = "info"
log.maxDays = 7
```

#### 5.3.2 客户端配置（frpc.toml）

```toml
# FRP客户端配置
serverAddr = "your-ecs-ip"
serverPort = 7000

# 认证配置
auth.token = "your-auth-token"

# 代理配置
[[proxies]]
name = "api"
type = "tcp"
localIP = "127.0.0.1"
localPort = 6000
remotePort = 6000

[[proxies]]
name = "llm"
type = "tcp"
localIP = "127.0.0.1"
localPort = 6002
remotePort = 6002

[[proxies]]
name = "mail"
type = "tcp"
localIP = "127.0.0.1"
localPort = 6003
remotePort = 6003

[[proxies]]
name = "frontend"
type = "tcp"
localIP = "127.0.0.1"
localPort = 6005
remotePort = 6005

[[proxies]]
name = "nas"
type = "tcp"
localIP = "127.0.0.1"
localPort = 6006
remotePort = 6006

[[proxies]]
name = "ddns"
type = "tcp"
localIP = "127.0.0.1"
localPort = 6007
remotePort = 6007
```

### 5.4 Docker配置

#### 5.4.1 Docker Compose配置

```yaml
version: '3.8'

services:
  api:
    build: ./api
    ports:
      - "6000:6000"
    environment:
      - NODE_ENV=production
      - PORT=6000
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    depends_on:
      - redis
    restart: unless-stopped

  mail:
    build: ./mail
    ports:
      - "6003:6003"
    environment:
      - NODE_ENV=production
      - PORT=6003
    restart: unless-stopped

  llm:
    build: ./llm
    ports:
      - "6002:6002"
    environment:
      - NODE_ENV=production
      - PORT=6002
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "6005:6005"
    environment:
      - VITE_API_BASE_URL=http://localhost:6000
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    restart: unless-stopped

volumes:
  redis-data:
```

#### 5.4.2 Dockerfile示例

```dockerfile
# API服务Dockerfile
FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --only=production

COPY . .

EXPOSE 6000

CMD ["npm", "start"]
```

---

## 6. 验证与测试

### 6.1 健康检查

#### 6.1.1 服务健康检查

```bash
# 使用健康检查脚本
./scripts/health-check.sh

# 或手动检查各服务
curl http://localhost:6000/health
curl http://localhost:6002/health
curl http://localhost:6003/health
curl http://localhost:6005
```

#### 6.1.2 FRP连接检查

```bash
# 检查FRP客户端状态
systemctl status frpc

# 查看FRP客户端日志
journalctl -u frpc -n 50

# 访问FRP管理面板
open http://your-ecs-ip:7500
```

#### 6.1.3 数据库连接检查

```bash
# 检查Redis连接
redis-cli ping

# 查看Redis信息
redis-cli info

# 检查Redis数据
redis-cli keys "*"
```

### 6.2 功能测试

#### 6.2.1 API接口测试

```bash
# 测试API健康接口
curl http://localhost:6000/health

# 测试API版本接口
curl http://localhost:6000/api/version

# 测试API认证接口
curl -X POST http://localhost:6000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"password"}'
```

#### 6.2.2 前端界面测试

```bash
# 访问前端管理平台
open http://localhost:6005

# 测试登录功能
# 测试API调用
# 测试界面交互
```

#### 6.2.3 邮箱服务测试

```bash
# 测试邮箱服务健康接口
curl http://localhost:6003/health

# 测试发送邮件接口
curl -X POST http://localhost:6003/api/mail/send \
  -H "Content-Type: application/json" \
  -d '{"to":"test@example.com","subject":"Test","body":"Test email"}'
```

#### 6.2.4 LLM服务测试

```bash
# 测试LLM服务健康接口
curl http://localhost:6002/health

# 测试LLM对话接口
curl -X POST http://localhost:6002/api/llm/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"Hello"}'
```

### 6.3 性能测试

#### 6.3.1 压力测试

```bash
# 使用Apache Bench进行压力测试
ab -n 1000 -c 10 http://localhost:6000/health

# 使用wrk进行压力测试
wrk -t4 -c100 -d30s http://localhost:6000/health
```

#### 6.3.2 负载测试

```bash
# 使用JMeter进行负载测试
# 配置测试计划
# 执行测试
# 分析结果
```

### 6.4 安全测试

#### 6.4.1 端口扫描

```bash
# 使用nmap扫描开放端口
nmap -p 6000-6007 localhost

# 检查端口安全性
netstat -tlnp | grep 600
```

#### 6.4.2 漏洞扫描

```bash
# 使用Nessus进行漏洞扫描
# 或使用OpenVAS进行开源漏洞扫描
```

---

## 7. 故障排除

### 7.1 常见问题

#### 7.1.1 服务启动失败

**问题描述**：服务无法启动

**排查步骤**：

```bash
# 1. 检查服务状态
systemctl status <service-name>

# 2. 查看服务日志
journalctl -u <service-name> -n 100

# 3. 检查端口占用
netstat -tlnp | grep <port>
lsof -i :<port>

# 4. 检查配置文件
cat /etc/<service>/<service>.conf

# 5. 检查环境变量
env | grep <service>
```

**解决方案**：

```bash
# 1. 停止占用端口的进程
kill -9 <pid>

# 2. 修复配置文件
vim /etc/<service>/<service>.conf

# 3. 重启服务
systemctl restart <service-name>
```

#### 7.1.2 FRP连接失败

**问题描述**：FRP客户端无法连接到服务端

**排查步骤**：

```bash
# 1. 检查FRP服务端状态
systemctl status frps

# 2. 检查FRP客户端状态
systemctl status frpc

# 3. 检查网络连接
ping <ecs-ip>
telnet <ecs-ip> 7000

# 4. 检查防火墙规则
iptables -L -n
firewall-cmd --list-all

# 5. 检查FRP配置
cat /etc/frp/frps.toml
cat /etc/frp/frpc.toml
```

**解决方案**：

```bash
# 1. 开放防火墙端口
firewall-cmd --permanent --add-port=7000/tcp
firewall-cmd --reload

# 2. 修复FRP配置
vim /etc/frp/frpc.toml

# 3. 重启FRP服务
systemctl restart frps
systemctl restart frpc
```

#### 7.1.3 端口冲突

**问题描述**：端口已被占用

**排查步骤**：

```bash
# 1. 查找占用端口的进程
lsof -i :<port>
netstat -tlnp | grep <port>

# 2. 查看进程详情
ps aux | grep <pid>
```

**解决方案**：

```bash
# 1. 停止占用端口的进程
kill -9 <pid>

# 2. 或修改服务端口
vim .env.local
# 修改对应的端口配置

# 3. 重启服务
systemctl restart <service-name>
```

#### 7.1.4 数据库连接失败

**问题描述**：无法连接到Redis数据库

**排查步骤**：

```bash
# 1. 检查Redis服务状态
systemctl status redis
docker ps | grep redis

# 2. 测试Redis连接
redis-cli ping
redis-cli -h <host> -p <port> ping

# 3. 检查Redis配置
cat /etc/redis/redis.conf

# 4. 查看Redis日志
tail -f /var/log/redis/redis.log
```

**解决方案**：

```bash
# 1. 启动Redis服务
systemctl start redis
# 或
docker start redis

# 2. 修复Redis配置
vim /etc/redis/redis.conf

# 3. 重启Redis服务
systemctl restart redis
```

### 7.2 日志分析

#### 7.2.1 服务日志

```bash
# 查看服务日志
journalctl -u <service-name> -f

# 查看最近100行日志
journalctl -u <service-name> -n 100

# 查看特定时间段的日志
journalctl -u <service-name> --since "2026-01-07 00:00:00" --until "2026-01-07 23:59:59"
```

#### 7.2.2 应用日志

```bash
# 查看应用日志
tail -f /var/log/yyc3/app.log

# 查看错误日志
grep ERROR /var/log/yyc3/app.log

# 查看最近100行日志
tail -n 100 /var/log/yyc3/app.log
```

#### 7.2.3 Docker日志

```bash
# 查看Docker容器日志
docker logs -f <container-name>

# 查看最近100行日志
docker logs --tail 100 <container-name>

# 查看特定时间段的日志
docker logs --since 2026-01-07T00:00:00 --until 2026-01-07T23:59:59 <container-name>
```

### 7.3 性能优化

#### 7.3.1 系统资源优化

```bash
# 1. 检查系统资源使用情况
top
htop
free -h
df -h

# 2. 优化系统参数
vim /etc/sysctl.conf

# 添加以下配置
net.core.somaxconn = 1024
net.ipv4.tcp_max_syn_backlog = 2048
net.ipv4.tcp_tw_reuse = 1
vm.swappiness = 10

# 3. 应用配置
sysctl -p
```

#### 7.3.2 应用性能优化

```bash
# 1. 启用Node.js集群模式
# 修改package.json
"scripts": {
  "start": "node cluster.js"
}

# 2. 启用Redis持久化
# 修改Redis配置
vim /etc/redis/redis.conf

# 添加以下配置
save 900 1
save 300 10
save 60 10000

# 3. 重启Redis服务
systemctl restart redis
```

#### 7.3.3 数据库优化

```bash
# 1. 优化Redis配置
vim /etc/redis/redis.conf

# 添加以下配置
maxmemory 2gb
maxmemory-policy allkeys-lru
timeout 300

# 2. 重启Redis服务
systemctl restart redis

# 3. 监控Redis性能
redis-cli info stats
redis-cli info memory
```

---

## 8. 维护与监控

### 8.1 日常维护

#### 8.1.1 服务监控

```bash
# 使用健康检查脚本
./scripts/health-check.sh

# 或使用监控工具
# - Prometheus + Grafana
# - Zabbix
# - Nagios
```

#### 8.1.2 日志管理

```bash
# 1. 配置日志轮转
vim /etc/logrotate.d/yyc3

# 添加以下配置
/var/log/yyc3/*.log {
    daily
    rotate 7
    compress
    delaycompress
    missingok
    notifempty
    create 0644 yyc3 yyc3
}

# 2. 测试日志轮转
logrotate -f /etc/logrotate.d/yyc3
```

#### 8.1.3 数据备份

```bash
# 1. 备份Redis数据
redis-cli BGSAVE

# 2. 备份Redis数据文件
cp /var/lib/redis/dump.rdb /backup/redis/dump.rdb.$(date +%Y%m%d)

# 3. 备份配置文件
tar -czf /backup/config/yyc3-config-$(date +%Y%m%d).tar.gz /etc/yyc3

# 4. 备份应用代码
tar -czf /backup/code/yyc3-code-$(date +%Y%m%d).tar.gz /opt/yyc3
```

### 8.2 监控告警

#### 8.2.1 系统监控

```bash
# 1. 安装监控工具
# 使用Prometheus + Grafana
docker run -d \
  --name prometheus \
  -p 9090:9090 \
  -v /opt/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml \
  prom/prometheus

docker run -d \
  --name grafana \
  -p 3000:3000 \
  grafana/grafana

# 2. 配置Prometheus
vim /opt/prometheus/prometheus.yml

# 添加以下配置
scrape_configs:
  - job_name: 'yyc3-api'
    static_configs:
      - targets: ['localhost:6000']
  - job_name: 'yyc3-mail'
    static_configs:
      - targets: ['localhost:6003']
  - job_name: 'yyc3-llm'
    static_configs:
      - targets: ['localhost:6002']
```

#### 8.2.2 告警配置

```bash
# 1. 配置告警规则
vim /opt/prometheus/alerts.yml

# 添加以下配置
groups:
  - name: yyc3_alerts
    rules:
      - alert: ServiceDown
        expr: up == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Service {{ $labels.instance }} is down"

      - alert: HighMemoryUsage
        expr: memory_usage > 80
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "High memory usage on {{ $labels.instance }}"

# 2. 配置告警通知
# 使用Alertmanager
docker run -d \
  --name alertmanager \
  -p 9093:9093 \
  -v /opt/alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml \
  prom/alertmanager
```

### 8.3 更新升级

#### 8.3.1 代码更新

```bash
# 1. 拉取最新代码
git pull origin main

# 2. 安装依赖
npm install

# 3. 构建应用
npm run build

# 4. 重启服务
./scripts/quick-restart.sh
```

#### 8.3.2 配置更新

```bash
# 1. 备份当前配置
cp .env.local .env.local.backup

# 2. 更新配置文件
vim .env.local

# 3. 验证配置
./scripts/validate-config.sh

# 4. 重启服务
./scripts/quick-restart.sh
```

#### 8.3.3 版本升级

```bash
# 1. 查看当前版本
git log -1

# 2. 拉取最新版本
git fetch --tags
git checkout v1.0.0

# 3. 执行升级脚本
./scripts/upgrade.sh

# 4. 验证升级
./scripts/health-check.sh
```

---

## 9. 附录

### 9.1 端口映射表

| 本地端口 | 远程端口 | 服务 | 说明 |
|---------|---------|------|------|
| 6000 | 6000 | API服务 | 主API接口服务 |
| 6002 | 6002 | LLM服务 | 大语言模型服务 |
| 6003 | 6003 | 邮箱服务 | 邮箱系统服务 |
| 6004 | 6004 | FRP服务 | FRP管理服务 |
| 6005 | 6005 | 前端服务 | 前端管理平台 |
| 6006 | 6006 | NAS服务 | NAS文件服务 |
| 6007 | 6007 | DDNS服务 | 动态域名解析服务 |
| 6379 | 6379 | Redis服务 | Redis数据库 |

### 9.2 环境变量参考

| 变量名 | 默认值 | 说明 |
|-------|-------|------|
| NODE_ENV | production | 运行环境 |
| PORT | 6000 | 服务端口 |
| HOST | 0.0.0.0 | 服务主机 |
| REDIS_HOST | localhost | Redis主机 |
| REDIS_PORT | 6379 | Redis端口 |
| JWT_SECRET | - | JWT密钥 |
| JWT_EXPIRES_IN | 7d | JWT过期时间 |
| LOG_LEVEL | info | 日志级别 |

### 9.3 常用命令

```bash
# 服务管理
./scripts/quick-start.sh      # 启动所有服务
./scripts/quick-stop.sh       # 停止所有服务
./scripts/quick-restart.sh    # 重启所有服务
./scripts/health-check.sh     # 健康检查

# Docker管理
docker-compose up -d          # 启动所有容器
docker-compose down           # 停止所有容器
docker-compose logs -f        # 查看日志
docker-compose ps             # 查看容器状态

# 系统管理
systemctl status <service>    # 查看服务状态
systemctl start <service>     # 启动服务
systemctl stop <service>      # 停止服务
systemctl restart <service>  # 重启服务
journalctl -u <service> -f    # 查看服务日志

# 网络管理
netstat -tlnp                 # 查看端口监听
lsof -i :<port>               # 查看端口占用
ping <host>                   # 测试网络连接
telnet <host> <port>          # 测试端口连通性
```

### 9.4 参考资料

- [YYC³ 团队智能应用开发标准规范](../YYC3-NAS-ECS-文档规范.md)
- [FRP官方文档](https://github.com/fatedier/frp)
- [Docker官方文档](https://docs.docker.com/)
- [Node.js官方文档](https://nodejs.org/docs/)
- [Redis官方文档](https://redis.io/documentation)

### 9.5 联系方式

- 项目地址：https://github.com/YYC-Cube/YYC3-NAS-ECS
- 问题反馈：https://github.com/YYC-Cube/YYC3-NAS-ECS/issues
- 邮箱联系：admin@0379.email

---

<div align="center">

> 「***YanYuCloudCube***」
> 「***<admin@0379.email>***」
> 「***Words Initiate Quadrants, Language Serves as Core for the Future***」
> 「***All things converge in the cloud pivot; Deep stacks ignite a new era of intelligence***」

</div>
