# =====================================================
# YYC³ 统一端口配置文件
# 创建日期: 2026-01-03
# 版本: 1.0.0
# 遵循YYC³标准：默认端口 6000-6007
# =====================================================

# -----------------------------------------------------
# 服务默认端口配置（开发环境）
# -----------------------------------------------------
# API服务
API_SERVICE_PORT=6000

# 邮件服务
MAIL_SERVICE_PORT=6003

# LLM服务
LLM_SERVICE_PORT=6002

# Redis API服务
REDIS_API_PORT=6379

# NAS服务
NAS_SERVICE_PORT=6004

# 管理服务
ADMIN_SERVICE_PORT=6001

# 监控服务
MONITOR_SERVICE_PORT=6006

# DDNS服务
DDNS_SERVICE_PORT=6007

# -----------------------------------------------------
# FRP映射端口配置（生产环境）
# -----------------------------------------------------
# 注意：所有生产服务通过frp暴露到统一的域名端口
FRP_VHOST_HTTP_PORT=18080
FRP_VHOST_HTTPS_PORT=4443

# 服务映射端口（本地端口 -> frp服务器端口）
# 这些端口对应frpc.toml中的localPort配置
FRP_API_LOCAL_PORT=6000
FRP_MAIL_LOCAL_PORT=6003
FRP_LLM_LOCAL_PORT=6002
FRP_NAS_LOCAL_PORT=6004
FRP_ADMIN_LOCAL_PORT=6001
FRP_MONITOR_LOCAL_PORT=6006
FRP_DDNS_LOCAL_PORT=6007

# -----------------------------------------------------
# 服务域名配置
# -----------------------------------------------------
API_SERVICE_DOMAIN=api.0379.email
MAIL_SERVICE_DOMAIN=mail.0379.email
LLM_SERVICE_DOMAIN=llm.0379.email
NAS_SERVICE_DOMAIN=nas.0379.email
ADMIN_SERVICE_DOMAIN=admin.0379.email
MONITOR_SERVICE_DOMAIN=monitor.0379.email
DDNS_SERVICE_DOMAIN=ddns.0379.email

# -----------------------------------------------------
# FRP服务配置
# -----------------------------------------------------
FRP_SERVER_HOST=8.152.195.33
FRP_SERVER_PORT=7001
FRP_SERVER_TOKEN=yyc3_nas
FRP_WEB_SERVER_PORT=7500
FRP_WEB_SERVER_USER=yyc3
FRP_WEB_SERVER_PASSWORD=my151001
